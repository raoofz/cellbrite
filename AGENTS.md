# AGENTS.md

## Cursor Cloud specific instructions

This repository ("cellbrite") is currently an empty scaffold with only a `README.md`. There are no application services, dependencies, or build/test/lint tooling configured yet.

**Current state:**
- No source code, no `package.json`, no `requirements.txt`, no `Makefile`, no `Dockerfile`, no `docker-compose.yml`.
- No lint, test, or build commands are available.
- No services need to be started.

**When code is added**, update this section with:
- How to install dependencies and which package manager to use.
- How to run lint, tests, and builds.
- How to start development servers and any required backing services.
- Any non-obvious gotchas discovered during setup.
